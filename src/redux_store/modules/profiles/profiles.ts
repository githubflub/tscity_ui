const initial_state = {}
// Not used for now.
export default function profilesReducer(state = initial_state, action = { type: undefined }) {
   switch (action.type) {
      default:
         return state;
   }
}